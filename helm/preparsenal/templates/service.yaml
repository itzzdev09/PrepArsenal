apiVersion: v1
kind: Service
metadata:
  name: {{ include "preparsenal.fullname" . }}-web
  labels:
    {{- include "preparsenal.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "preparsenal.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
